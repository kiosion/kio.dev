{
  "private": true,
  "version": "0.0.0",
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.1.2"
  },
  "scripts": {
    "prepare": "test -d .husky || (husky install && chmod ug+x ./.husky/* && husky add ./.husky/pre-commit 'pnpm lint-staged')"
  }
}
