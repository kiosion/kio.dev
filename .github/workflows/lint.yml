name: Lint Check

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "svelte-app/**"
      - "sanity-cms/**"
      - ".github/workflows/lint.yml"

concurrency:
  group: lint-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    outputs:
      svelte: ${{ steps.filter.outputs.svelte }}
      sanity: ${{ steps.filter.outputs.sanity }}

    steps:
      - uses: actions/checkout@v4

      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            svelte:
              - "svelte-app/**"
            sanity:
              - "sanity-cms/**"

  lint-svelte:
    name: Lint Svelte
    needs: changes
    if: needs.changes.outputs.svelte == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0
          cache: pnpm

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm --filter svelte-app lint:check

  lint-sanity:
    name: Lint Sanity
    needs: changes
    if: needs.changes.outputs.sanity == 'true'
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          run_install: false

      - uses: actions/setup-node@v4
        with:
          node-version: 22.14.0
          cache: pnpm

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm --filter sanity-cms lint:check
